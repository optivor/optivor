apiVersion: v1
kind: Service
metadata:
  name: {{ include "optivor.fullname" . }}
  labels:
    {{- include "optivor.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: 8080
      protocol: TCP
      name: http
  selector:
    {{- include "optivor.selectorLabels" . | nindent 4 }}
